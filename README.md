# 📡 NetScan v1.0

**NetScan** — это многопоточный сетевой сканер и анализатор топологии локальной сети, разработанный на C++ с использованием фреймворка Qt 5.

Проект выполнен в рамках курсовой работы (2025). Приложение позволяет обнаруживать активные узлы в сети, определять их MAC-адреса, производителей оборудования и тип операционной системы, а также визуализировать структуру сети в виде графа.

![Status](https://img.shields.io/badge/Status-Completed-success)
![Platform](https://img.shields.io/badge/Platform-Linux-blue)
![License](https://img.shields.io/badge/License-MIT-green)


## 🚀 Основные возможности

* **🔍 Многопоточное сканирование:** Использование Raw Sockets (ICMP Echo Request) и ARP-запросов для быстрого обнаружения хостов.
* **💾 База данных PostgreSQL:** Хранение истории сканирований и списка известных устройств.
* **🔐 Безопасность:** Использование **Хранимых функций (Stored Functions)** для работы с данными (защита от SQL-инъекций) и конфигурация через `.env` файл.
* **🏷 Определение устройств:**
    * Автоматическое определение производителя по OUI (MAC-адресу).
    * Fingerprinting (определение типа ОС/устройства) на основе открытых портов.
* **📊 Визуализация:** Построение динамической карты топологии сети.
* **🌗 Интерфейс:** Поддержка светлой и темной тем оформления.
* **📂 Экспорт:** Сохранение отчетов в формате CSV.

---

## 🛠 Технический стек

* **Язык:** C++17
* **Фреймворк:** Qt 5.15 (Widgets, Network, Sql, Concurrent)
* **База данных:** PostgreSQL 15 (в Docker контейнере)
* **Сборка:** CMake / QMake
* **ОС:** Linux (Kali/Ubuntu) — требуется доступ к `/proc/net/arp` и Raw Sockets.

---

## ⚙️ Установка и запуск

### 1. Предварительные требования

Убедитесь, что у вас установлены:
* Docker
* Qt 5 Development Tools (`qt5-default`, `build-essential`)
* CMake
* g++

### 2. Запуск Базы Данных (Docker)

Проект использует PostgreSQL в контейнере. Выполните команду для создания и запуска базы (один раз):

```bash
docker run --name netscanner_db \
  -e POSTGRES_USER=user \
  -e POSTGRES_PASSWORD=password \
  -e POSTGRES_DB=network_scan \
  -p 5432:5432 \
  -d postgres:15
```

### 3. Настройка конфигурации

Для безопасного подключения создайте файл `.env` в папке сборки (рядом с будущим исполняемым файлом `NetScanner`).

**Содержимое `.env`:**
```env
DB_HOST=127.0.0.1
DB_NAME=network_scan
DB_USER=user
DB_PASSWORD=password
DB_PORT=5432
```

### 3. Сборка проекта

```bash
mkdir build && cd build
cmake ..
make
```

### 5. Запуск

⚠️ Внимание: Для работы с Raw Sockets (ICMP ping) требуются права суперпользователя.

```bash
sudo ./NetScanner
```

### 📂 Структура проекта


* src/ — Исходный код приложения:

*    main.cpp — Точка входа, инициализация темы оформления.

*    scanner.cpp — Сетевое сканирование (ICMP, ARP, анализ портов).

*    dbmanager.cpp — Взаимодействие с PostgreSQL (через QSqlDatabase).

*    mainwindow.cpp — Основное окно, таблица результатов и обработка событий.

*    topologywidget.cpp — Визуализация графа сети (QGraphicsScene).

* include/ — Заголовочные файлы:

*    db_schema.h — SQL-скрипты и хранимые функции (Stored Functions).

* images/ — Ресурсы (иконки для карты топологии).
